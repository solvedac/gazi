{
  "name": "workspace",
  "private": true,
  "scripts": {
    "ext:dev": "pnpm -F=@with-gazi/extension run dev",
    "ext:dev:firefox": "pnpm -F=@with-gazi/extension run dev:firefox"
  },
  "devDependencies": {
    "prettier": "^3.1.1"
  }
}
